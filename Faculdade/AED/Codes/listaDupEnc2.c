#include <stdio.h>
#include <stdlib.h>

typedef struct nodo {
  int inf;
  struct nodo *next;
  struct nodo *prev;
} nodo;

typedef nodo *lista;

void cria(lista *l){
    
}
int vazia(lista l){}
int tam(lista l){}
void ins(lista *l, int k, int v){}
int recup(lista l, int k){}
void ret(lista *l, int k){}
void destruir(lista l){}

int main() {
    lista l;

    return 0;
}